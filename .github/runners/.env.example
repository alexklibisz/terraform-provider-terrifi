# URL of the repository this runner will serve
# e.g. https://github.com/your-org/terraform-provider-terrifi
REPO_URL=https://github.com/your-org/terraform-provider-terrifi

# GitHub Personal Access Token (classic) with `repo` scope.
# The runner uses this to mint short-lived registration tokens automatically,
# so it stays registered across restarts without manual token rotation.
ACCESS_TOKEN=ghp_your-personal-access-token

# Optional overrides (defaults shown)
# RUNNER_GROUP=Default
# EPHEMERAL=true
