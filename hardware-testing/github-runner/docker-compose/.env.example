# URL of the repository this runner will serve
# e.g. https://github.com/your-org/terrifi
REPO_URL=https://github.com/your-org/terrifi

# Runner registration token — generate one at:
# https://github.com/<owner>/<repo>/settings/actions/runners/new
# Tokens expire after 1 hour; the runner stores its credentials after first registration.
RUNNER_TOKEN=your-runner-registration-token

# Optional overrides (defaults shown)
# RUNNER_NAME=terrifi-hardware-runner
# LABELS=self-hosted,terrifi-hardware-test
# RUNNER_GROUP=Default
# EPHEMERAL=false  # set to true for one-job-then-exit runners
