# Tailscale auth key — create one at https://login.tailscale.com/admin/settings/keys
# Use a reusable (non-ephemeral) key so the node persists across restarts.
TS_AUTHKEY=tskey-auth-...

# Hostname this machine appears as on your tailnet (default: unifi-controller)
TS_HOSTNAME=unifi-controller

# Timezone for the UniFi controller
TZ=America/New_York

# Tailscale IP of this host — required for UniFi device adoption.
# Leave blank on first start, then:
#   docker compose exec tailscale tailscale ip -4
# Set that IP here and restart:
#   docker compose restart unifi
SYSTEM_IP=

# Optional: increase heap for large networks (default: 1024M)
# JVM_MAX_HEAP_SIZE=2048M

# Optional: additional tailscale arguments, e.g. to advertise ACL tags
# TS_EXTRA_ARGS=--advertise-tags=tag:unifi
